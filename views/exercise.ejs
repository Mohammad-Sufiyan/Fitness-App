<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Excercise</title>
</head>
<body style="background-color: black;">
  <center><h3 class="card-title" style="margin-top: 30px;color: white;" id="timer"></h3></center>
    <div class="card border-dark mb-3" style="margin:50px;background-color:black">
        <img  style="width: 50%; height: 10%;margin:auto" class="card-img-top" src="<%= data.gif%>" alt="Card image cap">
        <div class="card-body" style="margin: auto;">
          <hr>
          <center><h5 class="card-title" style="color: white;"><%= data.name%></h5></center>
          <p class="card-text" style="color: white;">Descriptions : <%= data.description%></p>
          <p class="card-text" style="color: white;">Modified : <%= data.modified%></p>
            <center><button onclick="setInterval(countdown, 1000);" style="color: white;" class="btn btn-outline-dark">Start</button></center>
            <form action="/streak" method="GET">
              <center><button style="margin-top: 10px;color: white;" id="streakbtn" class="btn btn-outline-dark" type="submit" disabled>Get your Streak</button></center>
            </form>
        </div>
      </div>
      <script>
        var timeLeft = 30;
        var elem = document.getElementById('timer');
        function countdown() {
          if (timeLeft == -1) {
            enableStreak();
          } else {
            elem.innerHTML = timeLeft + ' seconds remaining';
          timeLeft--;
      }
    }
    function enableStreak() {
      var element = document.getElementById('streakbtn');
      element.disabled = false;
    }
      </script>     
</body>
</html>